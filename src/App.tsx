// Library imports
import { FC } from "react";

// Local imports
import { HomePage } from "./components";

const App: FC = () => {
  return (
    <div>
      <HomePage />
    </div>
  );
};

export default App;
